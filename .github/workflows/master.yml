name: master

on:
  # schedule:
  #   - cron: '*/15 * * * *'
  workflow_dispatch:
    inputs:
      testParam:
        description: 'Test parameter'     
        required: true
        default: '1'
  push:
    branches:
      - master

jobs:
  run_master:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ windows-latest, ubuntu-latest, macos-latest ]

    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
        
#     - name: Setup dotnet
#       uses: actions/setup-dotnet@v1
#       with:
#         dotnet-version: 6.0.200-preview.22055.15
        
#     - name: Setup Go environment
#       uses: actions/setup-go@v2.1.4
#       with:
#         go-version: "1.16.x"

    - run: echo $env:PATH
  
    - run: echo $PATH

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: pypy-3.7
        
    - run: echo $PATH
    - run: echo $pythonLocation
    - run: ls -la /opt/hostedtoolcache/PyPy/3.7.12/x64
    
    

     




