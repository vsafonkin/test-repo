name: CI

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: windows-2016

    steps:
    - uses: actions/checkout@v2

    - name: Test
      run: |
        ls C:\\Windows\sysWOW64
